# e156-ecosystem-starter — environment template
#
# Copy this file to `.env` and fill in real values before running.
# Never commit your .env (it is gitignored).

# Your GitHub username (used by portfolio scripts)
PORTFOLIO_GITHUB_USER=
